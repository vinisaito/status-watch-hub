case 'finish':
  await updateStatusFinal({ chamado, status: "finalizado" });
  await onFinalize(chamado, trimmedObservacao);
  toast({
    title: "✅ Chamado Finalizado",
    description: `Chamado ${chamado} foi finalizado com sucesso!`,
  });
  break;
