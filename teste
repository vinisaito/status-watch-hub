// dentro de useWebsocketTimers.ts

useEffect(() => {
  if (isConnected && watchedChamados.length > 0) {
    console.log("🔄 Solicitando estados iniciais via getState...");
    watchedChamados.forEach(chamado => {
      getState(chamado);
    });
  }
}, [isConnected, watchedChamados, getState]);
