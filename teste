useEffect(() => {
  if (isConnected) {
    console.log("🔌 Conectado, pedindo estado...");
    getState(10513539); // seu chamado de teste
  }
}, [isConnected, getState]);









ws.current.onmessage = (event) => {
  const message = JSON.parse(event.data);
  console.log("📩 Mensagem recebida do WS:", message);
  ...
};
