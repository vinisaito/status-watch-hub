ws.current.onopen = () => {
  clearTimeout(connectionTimeout);
  setIsConnected(true);
  reconnectAttempts.current = 0;

  console.log('✅ WebSocket conectado com sucesso');
  toast({
    title: "Conectado",
    description: "WebSocket conectado com sucesso",
  });

  // 🔥 Assim que conectar, solicitar o estado atual
  // Aqui você decide se busca todos os chamados ativos ou só um em específico
  // Exemplo pedindo o estado de todos os chamados que você conhece:
  timers.forEach((_, chamado) => {
    sendMessage({ action: 'getState', chamado });
  });
};
