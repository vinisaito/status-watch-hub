useEffect(() => {
  if (alertData && alertData.length > 0) {
    // Passa todos os IDs de chamados para o hook
    setWatchedChamados(alertData.map(a => parseInt(a.chamado)));
  }
}, [alertData, setWatchedChamados]);
