// dentro do ActionTable
const advanceEscalation = async (chamadoId: number, nextLevel: number, observacao: string) => {
  try {
    await fetch(`/api/chamados/${chamadoId}/advance`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        nextLevel,
        observacao,
      }),
    });

    console.log(`✅ Chamado ${chamadoId} avançado para o nível ${nextLevel}`);
    // 🔹 Atualiza estado local ou refaz fetch da tabela
    refreshChamados();
  } catch (err) {
    console.error("❌ Erro ao avançar escalação:", err);
  }
};