const { timers } = useWebsocketTimers();

const chamadoState = timers.get(chamado) || chamadoData;

<ModernTimerModal
  isOpen={isModalOpen}
  onClose={handleClose}
  chamado={chamado}
  currentLevel={currentLevel}
  remainingTime={remainingTime}
  chamadoData={chamadoState}   // 👈 agora vem do WebSocket se existir
  ...
/>